---
platform: "linux"
image_resource:
  type: "docker-image"
  source:
    repository: "alpine"
inputs:
  - name: "repo"
outputs:
  - name: "buildroot"
run:
  path: "repo/ci/images/release-blob/prepare/run.sh"
params:
  blob: ~
