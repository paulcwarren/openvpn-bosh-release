#!/bin/bash

cd /var/vcap/jobs/openvpn/etc

if [ -e client*.conf ] ; then
    rm -f client*.conf
fi

<% p('openvpn.client_config').each_with_index do | config, idx | %>
cat > client<%= idx+1 %>.conf << EOF
<%= config %>
EOF
<% end %>
